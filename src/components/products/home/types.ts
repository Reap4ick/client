export interface IProductItem {
    id: number;
    name: string;
    description?: string;
    price: number;
    image: string;
}

export interface ICategoryName {
    id: number;
    name: string;
}